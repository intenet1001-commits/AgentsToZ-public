# AgentsToZ 디자인 시스템

2026-09 리디자인 기준. 구현 정본은 `src/index.css`의 공통 토큰이며, 화면 구성은
`src/workspaceDesign.css`와 각 화면의 스타일을 따른다. 상세 구현 규칙은
[CLAUDE.md](CLAUDE.md)의 UI/UX 절에 있다.

## 색상과 테마

기본은 밝은 웜 스톤, 선택 가능한 다크는 따뜻한 검정이다. 하나의 코퍼 강조색을 쓴다.
테마 선택은 이 기기에 저장하며, 기존 `gray` / `dark` 저장값을 유지한다.

| 역할 | 토큰 | 용도 |
|---|---|---|
| 화면·표면 | `--bg`, `--surface`, `--raised`, `--sunken` | 창, 카드, 입력, 구분 영역 |
| 텍스트 | `--ink`, `--ink-2`, `--ink-3` | 본문, 보조 설명, 캡션 |
| 경계 | `--line`, `--line-2` | 구분선, 컨트롤·팝오버 테두리 |
| 강조 | `--accent`, `--accent-soft`, `--accent-line`, `--on-accent` | 선택, 주요 동작, 키보드 초점 |
| 상태 | `--ok`, `--danger`, `--info`, `--violet`, `--warn` | 실행, 중지·삭제, 동기화, AI·별명, 오래됨 |
| 떠 있는 요소 | `--scrim`, `--shadow`, `--shadow-lg` | 팝오버, 모달, 토스트 |
| 터미널 | `--term` | 두 테마 모두 어두운 CLI 표면 |

새 색상 리터럴을 화면마다 추가하지 않는다. 투명도는 `rgb(var(--accent-rgb) / .12)`처럼
RGB 토큰을 이용한다. 기존 `--bg-base`, `--text-primary`, `--ink-<hex>`, Tailwind 색상 이름은
공통 토큰과 연결되어 있으므로 개별 호출부의 이름을 일괄 교체할 필요가 없다.

보조 텍스트도 주요 중립 배경에서 4.5:1 이상의 대비를 유지한다. `--accent-line`은 경계용이며
키보드 초점에는 불투명한 `--accent`를 사용한다. 선택 상태와 상태색만으로 의미를 전달하지 않는다.

## 서체와 위계

- 산세리프: `--font-sans` — Manrope Variable, Pretendard, 시스템 한글 서체 순서.
- 고정폭: `--font-mono` — JetBrains Mono Variable, 시스템 고정폭 순서. 경로·ID·포트 등에 사용.
- 서체는 `@fontsource-variable` 패키지로 로컬 제공한다. CSS `@import`는 Tailwind 선언보다 앞에 둔다.
- 상세 제목 22px/800, 화면 제목 15px/700, 섹션 13px/700, 본문 13px, 보조 설명 11.5px.
- 모바일 입력은 최소 16px, 주요 터치 동작은 최소 44px 높이를 유지한다.

## 레이아웃과 상호작용

- 데스크톱: 264px 사이드바와 기본 56px 화면 헤더. 좁은 상세 영역의 헤더 동작은 다음 줄로 배치한다.
- 메뉴: 데이터·연결은 ‘도구 및 설정’, 배율·터미널·AI 실행·빌드는 ‘실행 도구’에 묶는다.
- 실제 콘텐츠 폭을 기준으로 반응한다. 창 너비에서 사이드바와 앱 배율을 제외한 공간을 확인한다.
- 프로젝트 메타데이터는 좁은 상세 영역에서 라벨, 값, 동작을 줄바꿈한다. 긴 경로는 읽을 수 있게 나눈다.
- AI 대화의 3열 배치는 대화 영역이 1000 CSS px 이상일 때 사용한다. 그 아래에서는 입력 영역을 먼저 보여준다.
- 팝오버는 트리거를 덮지 않고 화면 안에서 세로 스크롤한다. 다시 클릭하거나 Escape로 닫을 수 있다.
- 모바일 목록에서도 새 프로젝트를 추가할 수 있고, 목록 전용 표시가 설정·등록 모달을 숨기지 않아야 한다.
- 기본 HTML dialog는 최상위 레이어에 표시되므로 변환된 앱 크기 대신 실제 viewport 크기로 제한한다.
- xterm의 span·canvas·textarea·ANSI 색상을 일반 화면 CSS로 덮어쓰지 않는다.
- 움직임 줄이기 설정에서는 장식 애니메이션을 중단한다.

## 검증

`bun run typecheck`, `bun run test`, `bun run test:rust`와 격리 API를 이용한
`tests/smoke.mjs`로 확인한다. UI 검증은 두 테마, 작은 창과 모바일, 100/125/150% 배율,
키보드 초점, 긴 텍스트, 빈 상태를 포함한다. 스크린샷만으로 실제 CLI 실행·로그인·WAN까지
검증했다고 판단하지 않는다.

리디자인 후 점검 근거는 [디자인 검토 기록](docs/handoffs/runtime-ui-v378/design-audit/README.md)에 있다.

이전 가이드의 상세 예시는 [리디자인 전 보관 문서](docs/archive/design-system-pre-redesign.md)에 보존했다.
